language: python
python:
  - "3.4"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
before_script: export PYTHONPATH=$PYTHONPATH:$(pwd)
script: travis_wait 20 make test
